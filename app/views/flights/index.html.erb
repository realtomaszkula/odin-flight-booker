<%= render 'search_form' %>
<hr>

<% if @search_query %>
  <div class= "container">
    <row>
      <div class="col-md-6">
      <h3><span class="glyphicon glyphicon-plane" aria-hidden="true"></span> Results</h3>
      <%= render 'results' %>
      </div>
    </row>
  </div>
<% end %>